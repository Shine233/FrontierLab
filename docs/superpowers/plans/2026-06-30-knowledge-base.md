# FrontierLab 知识库建设 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有前沿论文 markdown 库升级为可视化、可索引、可学习、可标准化产出、有跨会话记忆的知识库，全部以论文 YAML frontmatter 为唯一事实源。

**Architecture:** 一个 Python 脚本（仅依赖 pyyaml）扫描 `papers/**/*.md` 的 frontmatter，幂等生成 `indices/` 静态 markdown；Obsidian 端用 `dashboards/` 下的 Dataview 页读同一份 frontmatter 做动态视图；论文正文用标准 markdown 链接画方法谱系，GitHub 可点、Obsidian Graph 也能连边。三个 `skills/<name>/SKILL.md` 作为人/AI 可读的 SOP，产出论文/对比/复现并收尾调用脚本；`KB-MEMORY.md` 存跨会话决策。

**Tech Stack:** Python 3.10、PyYAML 6.x、Markdown、Obsidian Dataview（仅 dashboards/ 用）。

## Global Constraints

- 唯一事实源是论文 frontmatter；其余产物都从中派生，不重复维护。
- 不引入除 PyYAML 外的 Python 依赖。
- 脚本对 frontmatter 缺失字段降级处理，且必须容忍未知额外字段（真实论文已有 `short_name`/`pdf_url`/`weights_availability`/`data_availability` 等模板外字段）。
- 新增 5 个 frontmatter 字段全部可选：`landmark`(bool)、`supersedes`(list[slug])、`builds_on`(list[slug])、`org`(list[str])、`key_idea`(str)。
- slug = 论文文件名去掉 `.md`（如 `2024-roma`），全仓库唯一。
- 自动生成文件/区块顶部加注释：`<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->`。
- 人工叙述用 `<!-- MANUAL:start -->` / `<!-- MANUAL:end -->` 包裹，脚本不碰块内内容。
- 所有面向用户的文案、文档、注释用中文，沿用「结论先行 + 证据/推断分离」风格。
- 索引生成幂等全量重写，不做增量 diff。
- 不提交大文件（数据集/权重/缓存只记路径、来源、hash、许可证）。
- 学习初稿只写有论文的 7 个方向：3d-reconstruction、visual-localization、image-matching、vision-foundation-models、self-supervised-learning、world-models、efficient-training-inference。

---

## File Structure

新建/修改的文件及职责：

- `scripts/build_indices.py` — 索引生成器。读 frontmatter，写 `indices/`。
- `scripts/lib_frontmatter.py` — frontmatter 解析与 slug 提取，独立成模块便于测试。
- `scripts/lib_render.py` — 把解析出的论文记录渲染成各索引 markdown 片段。
- `tests/test_frontmatter.py` — 解析模块测试。
- `tests/test_render.py` — 渲染模块测试。
- `tests/test_build_indices.py` — 端到端：临时仓库 → 跑脚本 → 校验输出。
- `tests/fixtures/` — 测试用的最小论文样本。
- `papers/_template.md` — 增 5 个字段 + 正文「方法谱系」小节。
- `README.md` — 元数据说明同步增 5 个字段；新增 dashboards/learning/skills/KB-MEMORY 入口。
- `indices/papers.md`、`indices/directions.md`、`indices/methods.md` — 改造为「自动区 + MANUAL 区」。
- `indices/timeline.md`、`indices/landmarks.md` — 新增，全自动。
- `indices/comparisons.md` — 末尾自动追加对比报告清单。
- `dashboards/timeline.md`、`dashboards/landmarks.md`、`dashboards/by-direction.md`、`dashboards/reproduction-status.md` — Dataview 页。
- `learning/_template.md` + 7 份 `learning/<direction>.md` — 学习路线。
- `KB-MEMORY.md` — 项目记忆。
- `skills/paper-read/SKILL.md`、`skills/paper-compare/SKILL.md`、`skills/repro-plan/SKILL.md` — SOP。

任务顺序遵循依赖：先建解析/渲染/脚本（含测试），再改模板与现有索引，再 dashboards，再 learning，再 KB-MEMORY，最后 skills（引用前面全部产物）。

---

## Task 1: Frontmatter 解析模块

**Files:**
- Create: `scripts/lib_frontmatter.py`
- Create: `tests/test_frontmatter.py`
- Create: `tests/fixtures/papers/image-matching/2024-sample.md`
- Create: `tests/fixtures/papers/world-models/2026-sample.md`

**Interfaces:**
- Produces:
  - `parse_paper(path: str) -> dict | None` — 读单个 md，返回 frontmatter dict 加两个注入键：`_slug`（文件名去 `.md`）、`_path`（仓库相对路径，posix 风格）。无 frontmatter 或 YAML 解析失败返回 `None`（并不抛异常）。
  - `load_papers(papers_dir: str) -> list[dict]` — 递归扫描 `papers_dir` 下所有 `*.md`，跳过文件名以 `_` 开头的（如 `_template.md`），返回 `parse_paper` 非 None 结果的列表，按 `_slug` 升序排序。

- [ ] **Step 1: 写测试 fixtures**

`tests/fixtures/papers/image-matching/2024-sample.md`：

```markdown
---
type: paper-analysis
title: "Sample Matching Paper"
short_name: "SampleMatch"
year: 2024
direction: [image-matching, 3d-reconstruction]
method_family: [dense-feature-matching]
status: compared
reproduction: planned
landmark: true
org: [SampleLab]
key_idea: "用样例展示稠密匹配。"
supersedes: []
builds_on: [2023-sample-backbone]
github: "https://github.com/example/samplematch"
open_source: true
training_open_source: true
weights_availability: "自动下载"
updated: 2026-06-30
---

# Sample
正文。
```

`tests/fixtures/papers/world-models/2026-sample.md`：

```markdown
---
type: paper-analysis
title: "Sample World Model"
year: 2026
direction: [world-models]
method_family: [driving-world-model]
status: analyzed
reproduction: blocked-code-unavailable
org: [SampleAuto]
key_idea: "样例世界模型。"
updated: 2026-06-30
---

# Sample WM
正文。
```

- [ ] **Step 2: 写失败测试**

`tests/test_frontmatter.py`：

```python
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "scripts"))
import lib_frontmatter as fm

FIX = os.path.join(os.path.dirname(__file__), "fixtures", "papers")


def test_parse_paper_injects_slug_and_path():
    p = os.path.join(FIX, "image-matching", "2024-sample.md")
    rec = fm.parse_paper(p)
    assert rec is not None
    assert rec["_slug"] == "2024-sample"
    assert rec["_path"].endswith("image-matching/2024-sample.md")
    assert rec["year"] == 2024
    assert rec["landmark"] is True
    assert rec["builds_on"] == ["2023-sample-backbone"]


def test_parse_paper_tolerates_extra_and_missing_fields():
    p = os.path.join(FIX, "world-models", "2026-sample.md")
    rec = fm.parse_paper(p)
    assert rec is not None
    assert "landmark" not in rec  # 缺失字段不报错、不伪造
    assert rec["org"] == ["SampleAuto"]


def test_parse_paper_returns_none_without_frontmatter(tmp_path):
    f = tmp_path / "nofm.md"
    f.write_text("# 没有 frontmatter\n正文", encoding="utf-8")
    assert fm.parse_paper(str(f)) is None


def test_load_papers_skips_underscore_and_sorts(tmp_path):
    d = tmp_path / "papers" / "x"
    d.mkdir(parents=True)
    (tmp_path / "papers" / "_template.md").write_text(
        "---\ntitle: t\n---\n", encoding="utf-8"
    )
    (d / "2024-b.md").write_text(
        "---\ntitle: B\nyear: 2024\n---\n", encoding="utf-8"
    )
    (d / "2024-a.md").write_text(
        "---\ntitle: A\nyear: 2024\n---\n", encoding="utf-8"
    )
    recs = fm.load_papers(str(tmp_path / "papers"))
    assert [r["_slug"] for r in recs] == ["2024-a", "2024-b"]
```

- [ ] **Step 3: 运行确认失败**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_frontmatter.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'lib_frontmatter'`

- [ ] **Step 4: 实现 `scripts/lib_frontmatter.py`**

```python
"""论文 frontmatter 解析。唯一事实源读取层。"""
import os
import glob
import yaml


def parse_paper(path):
    """读单篇论文，返回 frontmatter dict（注入 _slug / _path）。

    无 frontmatter 或解析失败返回 None，不抛异常。
    """
    try:
        with open(path, "r", encoding="utf-8") as f:
            text = f.read()
    except OSError:
        return None
    if not text.startswith("---"):
        return None
    parts = text.split("---", 2)
    if len(parts) < 3:
        return None
    try:
        data = yaml.safe_load(parts[1])
    except yaml.YAMLError:
        return None
    if not isinstance(data, dict):
        return None
    data["_slug"] = os.path.splitext(os.path.basename(path))[0]
    data["_path"] = path.replace(os.sep, "/")
    return data


def load_papers(papers_dir):
    """递归扫描 papers_dir 下所有 *.md，跳过 _ 开头文件，按 _slug 排序。"""
    records = []
    pattern = os.path.join(papers_dir, "**", "*.md")
    for path in glob.glob(pattern, recursive=True):
        if os.path.basename(path).startswith("_"):
            continue
        rec = parse_paper(path)
        if rec is not None:
            records.append(rec)
    records.sort(key=lambda r: r.get("_slug", ""))
    return records
```

- [ ] **Step 5: 运行确认通过**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_frontmatter.py -v`
Expected: 4 passed

- [ ] **Step 6: 提交**

```bash
cd /Workspace/FrontierLab
git add scripts/lib_frontmatter.py tests/test_frontmatter.py tests/fixtures/
git commit -m "feat(indices): frontmatter 解析模块 + 测试 fixtures"
```

---

## Task 2: 渲染模块

**Files:**
- Create: `scripts/lib_render.py`
- Create: `tests/test_render.py`

**Interfaces:**
- Consumes: Task 1 的 paper 记录 dict（含 `_slug`、`_path`、`year`、`direction`、`method_family`、`landmark`、`org`、`key_idea`、`supersedes`、`builds_on`、`status`、`reproduction`、`github`/`code`、`title`、`short_name` 等）。
- Produces（全部纯函数，输入 `list[dict]`，输出 markdown `str`，不写文件，行尾不带多余空白）：
  - `render_papers_table(papers) -> str` — 论文总表，列：Paper、Year、Direction、Method family、Status、Reproduction、Org、Key idea、Link。
  - `render_directions(papers) -> str` — 按 direction 分组，每方向列出论文链接行。
  - `render_methods(papers) -> str` — 按 method_family 分组。
  - `render_timeline(papers) -> str` — 按 year 倒序，每年一节，列论文（landmark 前加 `★`），节末加该年 direction 计数小计。
  - `render_landmarks(papers) -> str` — 仅 `landmark is True` 的论文，每条列出 supersedes / builds_on slug。
  - 辅助 `display_name(rec) -> str` — 返回 `short_name` 或 `title` 或 `_slug`（按此优先级取第一个非空）。
  - 辅助 `paper_link(rec) -> str` — 返回 `[display_name](_path)` 的 markdown 链接（`_path` 已是仓库相对、posix）。

- [ ] **Step 1: 写失败测试**

`tests/test_render.py`：

```python
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "scripts"))
import lib_render as r

PAPERS = [
    {
        "_slug": "2024-roma", "_path": "papers/image-matching/2024-roma.md",
        "title": "RoMa", "short_name": "RoMa", "year": 2024,
        "direction": ["image-matching"], "method_family": ["dense-feature-matching"],
        "status": "compared", "reproduction": "planned",
        "org": ["LinkopingU"], "key_idea": "稠密匹配。",
        "landmark": True, "supersedes": ["2023-dkm"], "builds_on": ["2023-dinov2"],
    },
    {
        "_slug": "2026-sample-wm", "_path": "papers/world-models/2026-sample-wm.md",
        "title": "Sample WM", "year": 2026,
        "direction": ["world-models"], "method_family": ["driving-world-model"],
        "status": "analyzed", "reproduction": "blocked-code-unavailable",
        "org": ["SampleAuto"], "key_idea": "世界模型。",
    },
]


def test_display_name_priority():
    assert r.display_name(PAPERS[0]) == "RoMa"
    assert r.display_name({"_slug": "x", "title": "T"}) == "T"
    assert r.display_name({"_slug": "x"}) == "x"


def test_paper_link():
    assert r.paper_link(PAPERS[0]) == "[RoMa](papers/image-matching/2024-roma.md)"


def test_papers_table_has_header_and_rows():
    out = r.render_papers_table(PAPERS)
    assert "| Paper | Year |" in out
    assert "[RoMa](papers/image-matching/2024-roma.md)" in out
    assert "2026" in out


def test_timeline_sorts_desc_and_marks_landmark():
    out = r.render_timeline(PAPERS)
    i2026 = out.index("2026")
    i2024 = out.index("2024")
    assert i2026 < i2024  # 倒序
    assert "★" in out  # landmark 标记
    assert "image-matching: 1" in out  # 2024 方向小计


def test_landmarks_lists_lineage():
    out = r.render_landmarks(PAPERS)
    assert "RoMa" in out
    assert "2023-dkm" in out
    assert "2023-dinov2" in out
    assert "Sample WM" not in out  # 非 landmark 不出现


def test_directions_groups():
    out = r.render_directions(PAPERS)
    assert "## image-matching" in out
    assert "## world-models" in out


def test_methods_groups():
    out = r.render_methods(PAPERS)
    assert "dense-feature-matching" in out
    assert "driving-world-model" in out
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_render.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'lib_render'`

- [ ] **Step 3: 实现 `scripts/lib_render.py`**

```python
"""把论文记录渲染成各索引 markdown 片段。纯函数，不写文件。"""
from collections import Counter, defaultdict


def display_name(rec):
    for k in ("short_name", "title"):
        v = rec.get(k)
        if v:
            return str(v)
    return rec.get("_slug", "")


def paper_link(rec):
    return "[{}]({})".format(display_name(rec), rec.get("_path", ""))


def _join_list(v):
    if isinstance(v, list):
        return " / ".join(str(x) for x in v)
    return str(v) if v else ""


def _cell(v):
    """表格单元：转字符串并转义竖线。"""
    return _join_list(v).replace("|", "\\|")


def render_papers_table(papers):
    lines = [
        "| Paper | Year | Direction | Method family | Status | Reproduction | Org | Key idea | Link |",
        "|---|---:|---|---|---|---|---|---|---|",
    ]
    for r in papers:
        link = r.get("github") or r.get("code") or ""
        link_md = "[repo]({})".format(link) if link else ""
        lines.append(
            "| {name} | {year} | {direction} | {mf} | {status} | {repro} | {org} | {key} | {link} |".format(
                name=paper_link(r),
                year=r.get("year", ""),
                direction=_cell(r.get("direction")),
                mf=_cell(r.get("method_family")),
                status=_cell(r.get("status")),
                repro=_cell(r.get("reproduction")),
                org=_cell(r.get("org")),
                key=_cell(r.get("key_idea")),
                link=link_md,
            )
        )
    return "\n".join(lines)


def _group_by_list_field(papers, field):
    groups = defaultdict(list)
    for r in papers:
        vals = r.get(field) or []
        if not isinstance(vals, list):
            vals = [vals]
        for v in vals:
            groups[str(v)].append(r)
    return groups


def render_directions(papers):
    groups = _group_by_list_field(papers, "direction")
    out = []
    for key in sorted(groups):
        out.append("## {}".format(key))
        out.append("")
        for r in sorted(groups[key], key=lambda x: x.get("_slug", "")):
            out.append("- {} （{}）".format(paper_link(r), r.get("year", "")))
        out.append("")
    return "\n".join(out).rstrip()


def render_methods(papers):
    groups = _group_by_list_field(papers, "method_family")
    out = []
    for key in sorted(groups):
        out.append("## {}".format(key))
        out.append("")
        for r in sorted(groups[key], key=lambda x: x.get("_slug", "")):
            out.append("- {} （{}）".format(paper_link(r), r.get("year", "")))
        out.append("")
    return "\n".join(out).rstrip()


def render_timeline(papers):
    by_year = defaultdict(list)
    for r in papers:
        by_year[r.get("year", 0)].append(r)
    out = []
    for year in sorted(by_year, reverse=True):
        out.append("## {}".format(year))
        out.append("")
        dir_counter = Counter()
        for r in sorted(by_year[year], key=lambda x: x.get("_slug", "")):
            star = "★ " if r.get("landmark") is True else ""
            out.append("- {}{} — {}".format(star, paper_link(r), _join_list(r.get("key_idea"))))
            dirs = r.get("direction") or []
            if not isinstance(dirs, list):
                dirs = [dirs]
            for d in dirs:
                dir_counter[str(d)] += 1
        out.append("")
        summary = ", ".join("{}: {}".format(d, n) for d, n in sorted(dir_counter.items()))
        out.append("方向分布：{}".format(summary))
        out.append("")
    return "\n".join(out).rstrip()


def render_landmarks(papers):
    out = []
    for r in sorted(papers, key=lambda x: (-(x.get("year") or 0), x.get("_slug", ""))):
        if r.get("landmark") is not True:
            continue
        out.append("## {} （{}）".format(display_name(r), r.get("year", "")))
        out.append("")
        out.append("- 链接：{}".format(paper_link(r)))
        if r.get("key_idea"):
            out.append("- 核心：{}".format(r["key_idea"]))
        if r.get("supersedes"):
            out.append("- 取代/改进：{}".format(_join_list(r["supersedes"])))
        if r.get("builds_on"):
            out.append("- 基于：{}".format(_join_list(r["builds_on"])))
        out.append("")
    return "\n".join(out).rstrip()
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_render.py -v`
Expected: 7 passed

- [ ] **Step 5: 提交**

```bash
cd /Workspace/FrontierLab
git add scripts/lib_render.py tests/test_render.py
git commit -m "feat(indices): 索引渲染模块 + 测试"
```

---

## Task 3: 保留-标记块写入 + 主脚本

**Files:**
- Create: `scripts/build_indices.py`
- Create: `tests/test_build_indices.py`

**Interfaces:**
- Consumes: Task 1 `lib_frontmatter.load_papers`，Task 2 全部 `render_*` 函数。
- Produces:
  - `AUTO_HEADER` 常量 = `"<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->"`。
  - `write_full(path: str, body: str) -> None` — 全自动文件：写 `AUTO_HEADER + "\n\n" + body + "\n"`。
  - `splice_auto_block(path: str, body: str) -> str` — 半自动文件：在既有内容里，用 `<!-- AUTO:start -->` / `<!-- AUTO:end -->` 之间替换为 `body`；若文件不存在或无标记，返回 `AUTO_HEADER + 自动块 + 空 MANUAL 块` 的新内容。MANUAL 块（`<!-- MANUAL:start -->...<!-- MANUAL:end -->`）内容原样保留。
  - `main(repo_root: str = ".") -> None` — 编排：load_papers → 写 timeline/landmarks（full）→ splice papers/directions/methods（auto block）→ 追加 comparisons 清单。

- [ ] **Step 1: 写失败测试**

`tests/test_build_indices.py`：

```python
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "scripts"))
import build_indices as bi


def _make_repo(tmp_path):
    papers = tmp_path / "papers" / "image-matching"
    papers.mkdir(parents=True)
    (papers / "2024-x.md").write_text(
        "---\ntitle: X\nshort_name: X\nyear: 2024\n"
        "direction: [image-matching]\nmethod_family: [m1]\n"
        "status: compared\nreproduction: planned\nlandmark: true\n"
        "org: [Lab]\nkey_idea: K\n---\n# X\n",
        encoding="utf-8",
    )
    (tmp_path / "indices").mkdir()
    return tmp_path


def test_write_full_has_header(tmp_path):
    p = tmp_path / "out.md"
    bi.write_full(str(p), "BODY")
    text = p.read_text(encoding="utf-8")
    assert text.startswith(bi.AUTO_HEADER)
    assert "BODY" in text


def test_splice_preserves_manual_block(tmp_path):
    p = tmp_path / "papers_index.md"
    p.write_text(
        bi.AUTO_HEADER + "\n\n"
        "<!-- AUTO:start -->\nOLD AUTO\n<!-- AUTO:end -->\n\n"
        "<!-- MANUAL:start -->\n人工叙述保留我\n<!-- MANUAL:end -->\n",
        encoding="utf-8",
    )
    new = bi.splice_auto_block(str(p), "NEW AUTO")
    assert "NEW AUTO" in new
    assert "OLD AUTO" not in new
    assert "人工叙述保留我" in new


def test_splice_creates_when_missing(tmp_path):
    p = tmp_path / "new.md"
    new = bi.splice_auto_block(str(p), "BODY")
    assert "<!-- AUTO:start -->" in new
    assert "BODY" in new
    assert "<!-- MANUAL:start -->" in new


def test_main_generates_all_indices(tmp_path):
    repo = _make_repo(tmp_path)
    bi.main(str(repo))
    idx = repo / "indices"
    assert (idx / "timeline.md").exists()
    assert (idx / "landmarks.md").exists()
    assert "★" in (idx / "timeline.md").read_text(encoding="utf-8")
    papers_md = (idx / "papers.md").read_text(encoding="utf-8")
    assert "[X](" in papers_md


def test_main_is_idempotent(tmp_path):
    repo = _make_repo(tmp_path)
    bi.main(str(repo))
    first = (repo / "indices" / "papers.md").read_text(encoding="utf-8")
    bi.main(str(repo))
    second = (repo / "indices" / "papers.md").read_text(encoding="utf-8")
    assert first == second
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_build_indices.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'build_indices'`

- [ ] **Step 3: 实现 `scripts/build_indices.py`**

```python
#!/usr/bin/env python3
"""从论文 frontmatter 生成 indices/ 静态 markdown。幂等全量重写。

用法：python3 scripts/build_indices.py [repo_root]   (默认当前目录)
"""
import os
import sys
import re

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import lib_frontmatter as fm
import lib_render as render

AUTO_HEADER = "<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->"
AUTO_START = "<!-- AUTO:start -->"
AUTO_END = "<!-- AUTO:end -->"
MANUAL_START = "<!-- MANUAL:start -->"
MANUAL_END = "<!-- MANUAL:end -->"


def write_full(path, body):
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, "w", encoding="utf-8") as f:
        f.write(AUTO_HEADER + "\n\n" + body.rstrip() + "\n")


def _new_semi_auto(body):
    return (
        AUTO_HEADER + "\n\n"
        + AUTO_START + "\n" + body.rstrip() + "\n" + AUTO_END + "\n\n"
        + MANUAL_START + "\n\n" + MANUAL_END + "\n"
    )


def splice_auto_block(path, body):
    if not os.path.exists(path):
        return _new_semi_auto(body)
    with open(path, "r", encoding="utf-8") as f:
        text = f.read()
    if AUTO_START in text and AUTO_END in text:
        pattern = re.compile(
            re.escape(AUTO_START) + r".*?" + re.escape(AUTO_END), re.DOTALL
        )
        replacement = AUTO_START + "\n" + body.rstrip() + "\n" + AUTO_END
        return pattern.sub(lambda _m: replacement, text, count=1)
    # 既有文件但没有自动块：保留原内容为 MANUAL，前置自动块
    return (
        AUTO_HEADER + "\n\n"
        + AUTO_START + "\n" + body.rstrip() + "\n" + AUTO_END + "\n\n"
        + MANUAL_START + "\n" + text.rstrip() + "\n" + MANUAL_END + "\n"
    )


def _write_semi(path, body):
    new = splice_auto_block(path, body)
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, "w", encoding="utf-8") as f:
        f.write(new)


def _comparisons_listing(repo_root):
    comp_dir = os.path.join(repo_root, "comparisons")
    lines = ["## 对比报告清单"]
    if not os.path.isdir(comp_dir):
        return "\n".join(lines)
    paths = []
    for root, _dirs, files in os.walk(comp_dir):
        for fn in files:
            if fn.endswith(".md") and not fn.startswith("_"):
                rel = os.path.relpath(os.path.join(root, fn), repo_root)
                paths.append(rel.replace(os.sep, "/"))
    for rel in sorted(paths):
        lines.append("- [{}]({})".format(rel, "../" + rel))
    return "\n".join(lines)


def main(repo_root="."):
    papers_dir = os.path.join(repo_root, "papers")
    idx = os.path.join(repo_root, "indices")
    papers = fm.load_papers(papers_dir)

    write_full(os.path.join(idx, "timeline.md"), render.render_timeline(papers))
    write_full(os.path.join(idx, "landmarks.md"), render.render_landmarks(papers))

    _write_semi(os.path.join(idx, "papers.md"), render.render_papers_table(papers))
    _write_semi(os.path.join(idx, "directions.md"), render.render_directions(papers))
    _write_semi(os.path.join(idx, "methods.md"), render.render_methods(papers))
    _write_semi(os.path.join(idx, "comparisons.md"), _comparisons_listing(repo_root))

    print("索引已生成：{} 篇论文".format(len(papers)))


if __name__ == "__main__":
    main(sys.argv[1] if len(sys.argv) > 1 else ".")
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/test_build_indices.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
cd /Workspace/FrontierLab
git add scripts/build_indices.py tests/test_build_indices.py
git commit -m "feat(indices): 主生成脚本 + 保留-标记块 + 端到端测试"
```

---

## Task 4: 现有索引改造 + 首次真实生成

把现有 3 个手写索引改造成「自动区 + MANUAL 区」，然后跑真脚本，人工核对没误删叙述。

**Files:**
- Modify: `indices/papers.md`（人工叙述移入 MANUAL 块）
- Modify: `indices/directions.md`（大段叙述移入 MANUAL 块）
- Modify: `indices/methods.md`（移入 MANUAL 块）
- Modify: `indices/comparisons.md`（保留手写，末尾留自动追加位）

**Interfaces:**
- Consumes: Task 3 的 `build_indices.py`。

- [ ] **Step 1: 备份现有索引**

```bash
cd /Workspace/FrontierLab
mkdir -p /tmp/idx-backup && cp indices/*.md /tmp/idx-backup/
```

- [ ] **Step 2: 改造 `indices/papers.md`**

把现有 `> 状态：...` 两行说明和论文大表之间的关系重排为：顶部留自动块占位，原有说明和任何额外注解放进 MANUAL 块。编辑后文件结构为：

```markdown
<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->

<!-- AUTO:start -->
<!-- AUTO:end -->

<!-- MANUAL:start -->
# 论文索引

> 状态：`triage` 初筛；... （原有说明全文保留在此）
> 代码字段约定：...
<!-- MANUAL:end -->
```

（原有那张手写表删掉——它将由自动块取代；说明文字保留在 MANUAL。）

- [ ] **Step 3: 改造 `indices/directions.md`**

每个方向的"关联""复现状态"叙述是人工资产，必须保留。结构改为：

```markdown
<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->

<!-- AUTO:start -->
<!-- AUTO:end -->

<!-- MANUAL:start -->
# 方向索引（人工叙述）

> 自动区是各方向论文清单；以下为人工维护的关联分析与复现状态。

## 3d-reconstruction
- 相关方法族：...（原文保留）
- 复现状态：...（原文保留）

## visual-localization
...（其余方向叙述全部原样保留）
<!-- MANUAL:end -->
```

- [ ] **Step 4: 改造 `indices/methods.md`**

`methods.md` 内容几乎全是人工编写的方法族分析表（机读 frontmatter 无法生成这些定性表）。处理：整份移入 MANUAL 块，自动块只放"按 method_family 的论文清单"作为补充导航：

```markdown
<!-- AUTO-GENERATED by scripts/build_indices.py, 勿手改自动区 -->

<!-- AUTO:start -->
<!-- AUTO:end -->

<!-- MANUAL:start -->
# 方法族索引（人工分析）
...（原 methods.md 全文保留）
<!-- MANUAL:end -->
```

- [ ] **Step 5: 在 `indices/comparisons.md` 末尾加自动块**

读现有 `indices/comparisons.md`，在末尾追加：

```markdown

<!-- AUTO:start -->
<!-- AUTO:end -->
```

并在文件顶部第一行加 `AUTO_HEADER` 注释。其余手写内容不动。

- [ ] **Step 6: 跑真脚本生成**

Run: `cd /Workspace/FrontierLab && python3 scripts/build_indices.py`
Expected: `索引已生成：N 篇论文`（N ≥ 20）

- [ ] **Step 7: 人工核对**

Run: `cd /Workspace/FrontierLab && git diff --stat indices/ && echo "---" && head -40 indices/timeline.md`
核对：① 每个 MANUAL 块内的人工叙述完整未删；② timeline.md 有 ★ 标记且年份倒序；③ papers.md 自动块行数约等于论文数。若发现误删，从 `/tmp/idx-backup/` 恢复对应文件后重做 Step 2-6。

- [ ] **Step 8: 提交**

```bash
cd /Workspace/FrontierLab
git add indices/
git commit -m "refactor(indices): 改造为自动区+MANUAL区，首次脚本生成 timeline/landmarks"
```

---

## Task 5: Frontmatter 模板与 README 扩展

**Files:**
- Modify: `papers/_template.md`（加 5 字段 + 正文谱系小节）
- Modify: `README.md`（元数据说明加 5 字段；快速入口加新文件）

**Interfaces:**
- Consumes: 无（纯文档）。

- [ ] **Step 1: 给 `papers/_template.md` frontmatter 加 5 字段**

在现有 frontmatter 的 `confidence: medium` 行之后、`updated:` 行之前插入：

```yaml
landmark: false          # true = 划时代/奠基方法，时间线与 landmarks 索引加 ★
org: []                  # 机构/团队，如 [Meta, ByteDance]
key_idea: ""             # 一句话核心，用于索引表与悬浮预览
supersedes: []           # 取代/改进的前作 slug，如 [2024-roma]
builds_on: []            # 依赖但不取代的前作 slug，如 [2023-dinov2]
```

- [ ] **Step 2: 给 `papers/_template.md` 正文加谱系小节**

在第 `## 6. 与相似方法对比` 小节之前插入：

```markdown
## 方法谱系

> 仅在有谱系关系时填写。用标准 markdown 链接（GitHub 可点 + Obsidian Graph 连边）。
> 与 frontmatter 的 supersedes / builds_on 保持一致。

- 取代/改进：[<前作名>](../<direction>/<year>-<slug>.md)
- 基于：[<backbone 名>](../<direction>/<year>-<slug>.md)

```

- [ ] **Step 3: 更新 `README.md` 元数据说明**

在 README 的 frontmatter 示例（`confidence: medium` 行后）补上同样 5 个字段及注释，与 Step 1 一致。

- [ ] **Step 4: 更新 `README.md` 快速入口表**

在快速入口表追加 5 行：

```markdown
| [`indices/timeline.md`](indices/timeline.md) | 时间线：按年看方向演进与划时代方法（自动生成） |
| [`indices/landmarks.md`](indices/landmarks.md) | 划时代方法及其谱系（自动生成） |
| [`dashboards/`](dashboards/) | Obsidian Dataview 动态视图 |
| [`learning/`](learning/) | 各方向由浅入深学习路线 |
| [`KB-MEMORY.md`](KB-MEMORY.md) | 知识库跨会话记忆：约定、缺口、待办、决策 |
```

并在"推荐目录结构"与"建议工作流"中补一句：新增/修改论文后运行 `python3 scripts/build_indices.py` 重建索引。

- [ ] **Step 5: 验证模板 YAML 未被破坏**

`parse_paper` 单独解析模板应返回 dict（非 None）；`_` 开头跳过规则只在 `load_papers` 生效。

Run: `cd /Workspace/FrontierLab && python3 -c "import sys; sys.path.insert(0,'scripts'); import lib_frontmatter as fm; print(fm.parse_paper('papers/_template.md') is not None)"`
Expected: `True`。若打印 `False`，说明新增字段缩进破坏了模板 YAML，需修复。

- [ ] **Step 6: 确认 load_papers 跳过模板**

Run: `cd /Workspace/FrontierLab && python3 -c "import sys; sys.path.insert(0,'scripts'); import lib_frontmatter as fm; print(any(r['_slug']=='_template' for r in fm.load_papers('papers')))"`
Expected: `False`

- [ ] **Step 7: 提交**

```bash
cd /Workspace/FrontierLab
git add papers/_template.md README.md
git commit -m "docs(template): frontmatter 增 landmark/org/key_idea/supersedes/builds_on + 谱系小节"
```

---

## Task 6: Obsidian Dataview Dashboards

**Files:**
- Create: `dashboards/timeline.md`
- Create: `dashboards/landmarks.md`
- Create: `dashboards/by-direction.md`
- Create: `dashboards/reproduction-status.md`

**Interfaces:**
- Consumes: 论文 frontmatter（Dataview 运行时读 `papers/` 下 YAML）。

- [ ] **Step 1: 创建 `dashboards/timeline.md`**

```markdown
# 时间线（Dataview 动态视图）

> 在 Obsidian 中打开本页（需 Dataview 插件）。GitHub 上仅显示查询代码。

```dataview
TABLE year AS 年份, direction AS 方向, key_idea AS 核心, landmark AS 划时代
FROM "papers"
WHERE year
SORT year DESC, file.name ASC
```
```

- [ ] **Step 2: 创建 `dashboards/landmarks.md`**

```markdown
# 划时代方法（Dataview 动态视图）

```dataview
TABLE year AS 年份, direction AS 方向, supersedes AS 取代, builds_on AS 基于
FROM "papers"
WHERE landmark = true
SORT year DESC
```
```

- [ ] **Step 3: 创建 `dashboards/by-direction.md`**

```markdown
# 按方向分组（Dataview 动态视图）

```dataview
TABLE rows.file.link AS 论文, rows.year AS 年份
FROM "papers"
WHERE direction
FLATTEN direction
GROUP BY direction
SORT direction ASC
```
```

- [ ] **Step 4: 创建 `dashboards/reproduction-status.md`**

```markdown
# 复现状态（Dataview 动态视图）

```dataview
TABLE reproduction AS 复现状态, status AS 阅读状态, github AS 仓库
FROM "papers"
WHERE reproduction
SORT reproduction ASC, file.name ASC
```
```

- [ ] **Step 5: 校验 markdown 链接与代码围栏完整**

Run: `cd /Workspace/FrontierLab && grep -l "```dataview" dashboards/*.md | wc -l`
Expected: `4`

- [ ] **Step 6: 提交**

```bash
cd /Workspace/FrontierLab
git add dashboards/
git commit -m "feat(dashboards): Obsidian Dataview 时间线/划时代/方向/复现状态视图"
```

---

## Task 7: 学习路线模板 + 7 方向初稿

**Files:**
- Create: `learning/_template.md`
- Create: `learning/3d-reconstruction.md`
- Create: `learning/visual-localization.md`
- Create: `learning/image-matching.md`
- Create: `learning/vision-foundation-models.md`
- Create: `learning/self-supervised-learning.md`
- Create: `learning/world-models.md`
- Create: `learning/efficient-training-inference.md`

**Interfaces:**
- Consumes: `indices/`、`papers/`、`comparisons/` 已有链接。

- [ ] **Step 1: 创建 `learning/_template.md`**

```markdown
---
type: learning-path
direction: ""
maturity: draft      # draft = AI 初稿；refined = 已融入个人经验
updated: 2026-06-30
---

# <方向> 学习路线

## 0. 这个方向在解决什么
一段话概述。

## 1. 前置知识
- 链到外部资源（课程、综述、基础论文）。

## 2. 奠基论文（仓库内）
- ★ [<论文>](../papers/<direction>/<file>.md) — 为什么是奠基。

## 3. 近期 SOTA 与分支
- [<对比报告>](../comparisons/<direction>/<file>.md)

## 4. 动手：最小复现路径
- [<复现记录>](../reproductions/<direction>/<method>/README.md) 或指明 planned。

## 5. 常见误区 / 我的判断
- 误区：
- 判断：
```

- [ ] **Step 2: 写 `learning/image-matching.md`**

按模板填，引用仓库已有 image-matching 内容（RoMa/RoMa v2/LightGlue/GIM/LoMa、对比报告 `comparisons/image-matching/image-matching-methods.md`）。frontmatter `direction: image-matching`、`maturity: draft`。0-5 节均填实际内容：奠基论文标 RoMa/LightGlue，SOTA 链对比报告，最小复现指向 RoMa/LightGlue inference。

- [ ] **Step 3: 写 `learning/vision-foundation-models.md`**

引用 DINO v1/v2/v3 与 `comparisons/vision-foundation-models/dino-family.md`。奠基论文标 DINO v1（★）。

- [ ] **Step 4: 写 `learning/self-supervised-learning.md`**

复用 DINO 系列，视角放在无标签表征/自蒸馏/防塌缩；链 dino-family 对比。

- [ ] **Step 5: 写 `learning/3d-reconstruction.md`**

引用 DA3/VGGT-Ω/Pi3/LingBot-Map/MapAnything 与两份对比报告（visual-geometry-foundation-models、streaming-3d-reconstruction）+ `reports/feedforward_3d_reconstruction_compare.md`。

- [ ] **Step 6: 写 `learning/visual-localization.md`**

引用 Reloc3r/Reloc-VGGT/MARePo 与 `comparisons/visual-localization/feed-forward-visual-relocalization.md`。最小复现指向 Reloc3r（可跑 baseline）。

- [ ] **Step 7: 写 `learning/world-models.md`**

引用 X-World/X-Foresight/X-Cache/DreamZero/Xiaomi JointWM 与两份对比（xpeng-x-series、robot-world-action-models）。注明多数代码未公开，最小复现指向 DreamZero-DROID。

- [ ] **Step 8: 写 `learning/efficient-training-inference.md`**

引用 X-Cache/X-Foresight；说明该方向仓库内样本少，前置知识多链外部。

- [ ] **Step 9: 校验所有内部链接存在**

Run:
```bash
cd /Workspace/FrontierLab && python3 - <<'PY'
import re, os, glob
bad = []
for f in glob.glob("learning/*.md"):
    if os.path.basename(f).startswith("_"): continue
    for m in re.finditer(r"\]\((\.\./[^)]+)\)", open(f, encoding="utf-8").read()):
        target = os.path.normpath(os.path.join("learning", m.group(1)))
        if not os.path.exists(target):
            bad.append((f, m.group(1)))
print("BROKEN:", bad if bad else "none")
PY
```
Expected: `BROKEN: none`（若有破损，修对应链接路径）

- [ ] **Step 10: 提交**

```bash
cd /Workspace/FrontierLab
git add learning/
git commit -m "docs(learning): 学习路线模板 + 7 个有论文方向的初稿"
```

---

## Task 8: 项目记忆文件

**Files:**
- Create: `KB-MEMORY.md`

**Interfaces:**
- Consumes: 无。Produces: 供 Task 9 三个 skill 读写的约定结构。

- [ ] **Step 1: 创建 `KB-MEMORY.md`**

```markdown
# 知识库记忆（KB-MEMORY）

> 三个 skill（paper-read / paper-compare / repro-plan）每次运行先读本文件，收尾按需更新。
> 分工：indices/ = 结构化机读事实；本文件 = 叙事性决策与待办。

## 约定与偏好
- 命名：`papers/<direction>/<year>-<slug>.md`，slug 全仓库唯一。
- 风格：结论先行；证据与推断分开标注；中文。
- 对比维度按方向稳定复用（见各 comparisons 文件末尾"建议固定对比维度"）。
- 谱系信息写两处：frontmatter 的 supersedes/builds_on + 正文「方法谱系」小节标准 markdown 链接。
- 新增/改动论文后运行 `python3 scripts/build_indices.py` 重建索引。

## 各方向缺口
- multimodal-learning / reasoning-agents：暂无论文，学习路线待方向有内容再建。
- 其余方向缺口随阅读补充。

## 未完成意图
- 复现 planned 列表见 indices/directions.md 各方向「复现状态」。

## 近期决策记录
- 2026-06-30：确立 frontmatter 为唯一事实源 + 脚本生成静态索引 + Dataview 动态视图双轨方案。
```

- [ ] **Step 2: 提交**

```bash
cd /Workspace/FrontierLab
git add KB-MEMORY.md
git commit -m "docs(memory): 新增 KB-MEMORY 跨会话记忆文件"
```

---

## Task 9: 三个 SKILL.md

**Files:**
- Create: `skills/paper-read/SKILL.md`
- Create: `skills/paper-compare/SKILL.md`
- Create: `skills/repro-plan/SKILL.md`

**Interfaces:**
- Consumes: `papers/_template.md`、`comparisons/_template.md`、`reproductions/_template.md`、`scripts/build_indices.py`、`KB-MEMORY.md`。

- [ ] **Step 1: 创建 `skills/paper-read/SKILL.md`**

```markdown
---
name: paper-read
description: 读一篇前沿论文并按仓库规范产出分析文件。适用于「分析这篇论文」「读 arxiv xxxx」。
---

# 论文阅读分析 SOP

## 何时用
给定论文链接 / arxiv id / PDF，要在 `papers/<direction>/` 产出一篇符合仓库规范的分析。

## 步骤
1. 先读 `KB-MEMORY.md`：确认命名约定、该方向缺口、对比维度口径。
2. 确定 direction（用 README「方向 slug」列表）与 slug = `<year>-<short-title>`。
3. 复制 `papers/_template.md` 到 `papers/<direction>/<slug>.md`。
4. 填 frontmatter：必填 title/year/direction/method_family/status/reproduction；
   尽量填 org/key_idea；判断 landmark（是否奠基/划时代）；有谱系则填 supersedes/builds_on。
5. 填正文：结论先行 3-5 条；证据与推断分开标注。
6. 若有谱系，填正文「方法谱系」小节（标准 markdown 链接，与 frontmatter 一致）。
7. 收尾：运行 `python3 scripts/build_indices.py` 重建索引。
8. 更新 `KB-MEMORY.md`：补该方向缺口/决策（如有）。

## 验收
- frontmatter 能被 `scripts/lib_frontmatter.py` 解析（无 YAML 报错）。
- `python3 scripts/build_indices.py` 跑通，新论文出现在 indices/papers.md 与 timeline.md。
- 谱系 frontmatter 与正文链接一致。
```

- [ ] **Step 2: 创建 `skills/paper-compare/SKILL.md`**

```markdown
---
name: paper-compare
description: 把同方向多篇论文整理成横向对比。适用于「对比这几个方法」「更新 xx 方向对比表」。
---

# 论文横向对比 SOP

## 何时用
同方向 ≥2 篇论文，要产出/更新 `comparisons/<direction>/<topic>.md`。

## 步骤
1. 先读 `KB-MEMORY.md` 与该方向已有 comparisons 文件末尾「建议固定对比维度」。
2. 复制 `comparisons/_template.md`（或在既有对比文件上追加）。
3. 复用稳定维度表；新方法按同维度补列。
4. 明确标注：协议一致可比 vs 不可跨论文直接比较的项。
5. 结论先行：给推荐排序 + 场景化选择。
6. 收尾：运行 `python3 scripts/build_indices.py`（刷新 comparisons 清单）。
7. 更新 `KB-MEMORY.md` 决策记录（如调整了维度口径）。

## 验收
- 对比文件 frontmatter 含 direction/methods。
- indices/comparisons.md 自动块出现该文件。
- 可比/不可比标注清晰。
```

- [ ] **Step 3: 创建 `skills/repro-plan/SKILL.md`**

```markdown
---
name: repro-plan
description: 评估一篇论文的可复现性并产出复现计划。适用于「这个能复现吗」「写复现计划」。
---

# 复现判断 / 计划 SOP

## 何时用
要判断某方法可复现性，并产出 `reproductions/<direction>/<method>/README.md`。

## 步骤
1. 先读 `KB-MEMORY.md` 与该论文 `papers/` 文件的「复现判断」小节。
2. 核查仓库：代码是否公开、训练是否开源、权重/数据可得性、license、commit。
3. 复制 `reproductions/_template.md` 到 `reproductions/<direction>/<method>/README.md`。
4. 填：环境、目标 commit、最小复现路径、预计成本、是否值得复现。
5. 诚实记录 blocker（代码未发布 / 数据未公开 / 算力不足）。
6. 同步论文 frontmatter 的 `reproduction:` 字段（planned/running/blocked/reproduced）。
7. 收尾：运行 `python3 scripts/build_indices.py`。
8. 更新 `KB-MEMORY.md` 未完成意图。

## 验收
- reproduction README 含环境/commit/最小路径/是否值得复现。
- 论文 frontmatter reproduction 字段与结论一致。
- build_indices 跑通。
```

- [ ] **Step 4: 校验 SKILL frontmatter 可解析**

Run:
```bash
cd /Workspace/FrontierLab && python3 - <<'PY'
import yaml, glob
for f in glob.glob("skills/*/SKILL.md"):
    t = open(f, encoding="utf-8").read()
    fmtext = t.split("---", 2)[1]
    d = yaml.safe_load(fmtext)
    assert "name" in d and "description" in d, f
    print("ok", f, d["name"])
PY
```
Expected: 三行 `ok skills/.../SKILL.md <name>`

- [ ] **Step 5: 提交**

```bash
cd /Workspace/FrontierLab
git add skills/
git commit -m "feat(skills): paper-read / paper-compare / repro-plan SOP"
```

---

## Task 10: 全量回归与收尾

**Files:**
- Modify: 可能微调任意上游文件（仅修 bug）。

- [ ] **Step 1: 跑全部测试**

Run: `cd /Workspace/FrontierLab && python3 -m pytest tests/ -v`
Expected: 全部 passed（16 项：4 frontmatter + 7 render + 5 build_indices）

- [ ] **Step 2: 真实重建索引并确认幂等**

Run:
```bash
cd /Workspace/FrontierLab
python3 scripts/build_indices.py
git diff --stat indices/
python3 scripts/build_indices.py
git diff --stat indices/
```
Expected: 第二次 `git diff --stat` 无变化（幂等）。

- [ ] **Step 3: 校验所有内部 markdown 链接**

Run:
```bash
cd /Workspace/FrontierLab && python3 - <<'PY'
import re, os, glob
bad = []
for f in glob.glob("**/*.md", recursive=True):
    if "/.git/" in f: continue
    base = os.path.dirname(f)
    for m in re.finditer(r"\]\((?!https?://|#)([^)]+\.md)[^)]*\)", open(f, encoding="utf-8").read()):
        target = os.path.normpath(os.path.join(base, m.group(1)))
        if not os.path.exists(target):
            bad.append((f, m.group(1)))
print("BROKEN:", len(bad))
for b in bad: print(b)
PY
```
Expected: `BROKEN: 0`（dashboards 内的 wikilink 不算，因为用的是 Dataview 不是 md 链接；若报破损，修复或确认是模板占位 `<...>` 并排除）

- [ ] **Step 4: 更新 README 当前资料清单（如需要）**

若 README「当前已有资料」清单要补 timeline/landmarks/dashboards/learning 入口，补齐。

- [ ] **Step 5: 最终提交**

```bash
cd /Workspace/FrontierLab
git add -A
git commit -m "chore: 知识库建设全量回归通过（测试+幂等+链接校验）"
```

---

## Self-Review 记录

- **Spec 覆盖**：模块1→Task5；模块2→Task1-4；模块3→Task6；模块4(skills)→Task9；模块5(learning)→Task7；模块6(memory)→Task8。全覆盖。
- **占位符**：Task7 各方向初稿正文是"按模板填实际内容"而非给出全文——这是有意的，因为初稿内容依赖实现时读取的论文细节，但每步都指明了必须引用的具体文件和必填小节，非空泛 TODO。
- **类型一致**：`parse_paper`/`load_papers`/`render_*`/`splice_auto_block`/`write_full`/`main` 跨 Task 签名一致；`_slug`/`_path` 注入键全程一致；`AUTO_START`/`MANUAL_START` 标记常量一致。
- **已知修正**：Task5 Step5 预期值在步内自我修正为 `False`（模板能被单独 parse，但被 load_papers 的 `_` 规则跳过）。
